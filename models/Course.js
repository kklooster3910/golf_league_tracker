const mongoose = require("mongoose");
const Schema = mongoose.Schema;

// adding comment for ssh key test

const CourseSchema = new Schema({});

module.exports = Course = mongoose.model("Course", CourseSchema);
